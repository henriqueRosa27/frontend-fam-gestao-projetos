import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  buttonRow: {
    display: "flex",
    justifyContent: "flex-end",
    paddingBottom: "1vh",
    marginBottom: "15px",
    marginTop: "15px",
  },
}));

export { useStyles };
